# 명세서: `src/common/__init__.py`

## 역할
- `common` 네임스페이스 패키지를 정의하고, 공용 유틸리티 모듈(`logger`, `load_config`, `expand_vars` 등)을 묶는 진입점이다.

## 핵심 규칙
- 임포트 시 부수 효과(환경 변수 변경, 파일 IO, 로깅 초기화 등)를 일으키지 않는다.
- 공용 심볼을 재노출하더라도 가벼운 바인딩 수준에 머물고, 무거운 초기화 로직은 각 모듈 내부에 유지한다.
